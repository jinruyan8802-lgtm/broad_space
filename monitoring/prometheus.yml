global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "broadspace-api"
    static_configs:
      - targets: ["api:8000"]
  - job_name: "broadspace-processor"
    static_configs:
      - targets: ["processor:8080"]